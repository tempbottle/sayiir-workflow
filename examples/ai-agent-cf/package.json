{
  "name": "ai-agent-cf",
  "private": true,
  "version": "0.0.0",
  "description": "Durable AI research agent on Cloudflare Workers, powered by Sayiir + Workers AI + D1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply sayiir-agent --local",
    "db:migrate:remote": "wrangler d1 migrations apply sayiir-agent --remote",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sayiir/cloudflare": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260301.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
