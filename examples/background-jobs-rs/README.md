# Background Jobs (Rust)

Email pipeline with retries, timeouts, and durable execution. Demonstrates
`#[task]` macro with retry configuration and `workflow!` composition.

Matches the [Background Jobs tutorial](https://docs.sayiir.dev/tutorials/background-jobs-rust/).

## Prerequisites

- Rust 1.85+

## Run

```bash
cargo run
```
