/**
 * Task definition for workflow steps.
 *
 * Re-exported from @sayiir/flow-js.
 * No `getTaskContext()` — WASM does not support the native context API.
 */

export { task } from "@sayiir/flow-js";
export type { TaskFn } from "@sayiir/flow-js";
